services:
  - type: web
    name: heybuddy-backend
    env: node
    region: oregon
    buildCommand: npm install && npm --prefix server install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: CLIENT_ORIGIN
        value: "*"

